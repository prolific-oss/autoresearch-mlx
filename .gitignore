.DS_Store
.venv/
__pycache__/
*.pyc
*.log
run.log
surfaces.txt
surface-verify-prompt.md
build-2-closeout.md

# Model weights and training artifacts (zipped + shared separately)
*.safetensors
*.pt
*.snapshot.json
checkpoints/
prod_logs/

# Pipeline output dirs — gitignored by default but eval_data/phase2/ is the
# Phase 2 handoff payload (tracked) and report/ has earlier diagnostic UIs.
eval_data/*
!eval_data/phase2/
eval_data/phase2/_PREVIEW*
eval_data/phase2/pairs_for_claude.txt
eval_data/phase2/prolific_generations_PREVIEW.json
report/

# Pipeline outputs (regenerated)
annotations_*.json
auto_metrics.json
auto_metrics.tsv
pairs*.jsonl

# GPU-side autoresearch tooling (separate workflow, not for handoff)
autoresearch-pytorch/

# Workspace junk
.claude/
_old_*/
